import React from "react";

function Header(){
    return(
        <header className="header">
            <h1 id="header_text">
                Clock App
            </h1>
        </header>
    )
}

export default Header;